{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "postbuild": "php ../../artisan vendor:publish --tag=noerd-modal-assets --force --ansi"
    },
    "dependencies": {
        "laravel-vite-plugin": "^1.0",
        "vite": "6.3.4"
    }
}
